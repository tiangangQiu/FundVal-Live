name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            platform: macOS
          - os: windows-latest
            platform: Windows
          - os: ubuntu-latest
            platform: Linux

    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install uv (Unix)
        if: runner.os != 'Windows'
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Install uv (Windows)
        if: runner.os == 'Windows'
        run: irm https://astral.sh/uv/install.ps1 | iex
        shell: powershell

      - name: Install Python dependencies
        run: |
          uv pip install --system -r backend/requirements.txt
          uv pip install --system pyinstaller

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Build backend with PyInstaller
        run: uv run pyinstaller backend.spec --clean

      - name: Install Electron dependencies
        run: npm ci

      - name: Build Electron app (macOS arm64)
        if: runner.os == 'macOS'
        run: npm run dist -- --arm64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron app (macOS x64)
        if: runner.os == 'macOS'
        run: npm run dist -- --x64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron app (Windows)
        if: runner.os == 'Windows'
        run: npm run dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron app (Linux)
        if: runner.os == 'Linux'
        run: npm run dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: release/*.dmg
          if-no-files-found: error

      - name: Upload artifacts (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: release/*.exe

      - name: Upload artifacts (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: |
            release/*.AppImage
            release/*.deb

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          body: |
            ## 安装包下载

            ### macOS
            - **Apple Silicon (M1/M2/M3)**: `FundVal-Live-*-arm64.dmg`
            - **Intel**: `FundVal-Live-*-x64.dmg`

            ### Windows
            - **64位**: `FundVal-Live-Setup-*.exe`

            ### Linux
            - **AppImage**: `FundVal-Live-*.AppImage`
            - **Debian/Ubuntu**: `fundval-live_*_amd64.deb`

            ## 本次更新 (v1.1.1)

            ### 🐛 重要修复

            - **修复跨浏览器/跨设备数据不互通问题**
              - 用户偏好（自选列表、当前账户、排序选项）从 localStorage 迁移到后端数据库
              - 换浏览器、换设备、换端口访问，数据完全互通
              - 自动从旧版本 localStorage 迁移数据到后端
              - Docker 部署用户不再出现"换浏览器数据为空"的问题

            ### 🔧 技术改进

            - 新增 `/api/preferences` 接口，统一管理用户偏好
            - 前端启动时自动从后端加载偏好，修改时实时同步
            - 兼容旧版本数据，首次启动自动迁移

            ---

            ## 历史更新 (v1.1.0)

            ### ✨ 新功能

            - **自定义 AI 提示词**
              - 在设置页面管理 AI 提示词模板
              - 支持创建、编辑、删除、设为默认
              - AI 分析时可选择不同的提示词模板
              - 内置变量占位符（基金名称、代码、净值、涨跌幅、历史数据）
              - 支持自定义分析风格（Linus 风格、温和风格等）

            - **数据导入导出**
              - 支持导出用户数据为 JSON 格式
              - 可选择性导出/导入模块（账户、持仓、交易记录、AI 提示词、订阅设置、系统设置）
              - 两种导入模式：合并模式（保留现有数据）、替换模式（清空后导入）
              - 敏感数据自动脱敏（API Key、密码）
              - 外键约束验证，确保数据完整性
              - 详细的导入结果反馈

            - **所有账户聚合视图**
              - 新增「所有账户」选项，查看全部持仓汇总
              - 自动合并相同基金的持仓
              - 显示总成本、总市值、总收益

            - **启动错误友好提示**
              - 启动失败时显示 GUI 错误对话框
              - 智能识别常见错误（端口冲突、数据库错误、权限错误）
              - 提供具体的解决方法
              - 防止控制台窗口一闪而过

            ### 🔧 优化改进

            - **安全性增强**
              - 添加请求大小限制（10MB）
              - 数据导入/导出模块白名单验证
              - 防止注入攻击

            - **数据完整性**
              - AI 提示词名称唯一约束
              - 导入时按依赖顺序处理（settings → ai_prompts → accounts → positions → transactions → subscriptions）
              - 事务原子性保证（全部成功或全部失败）

            - **用户体验**
              - 导入时显示详细的成功/跳过/失败/删除统计
              - 替换模式需要二次确认（输入"确认删除"）
              - 错误信息更友好，直接显示具体问题

            ### 🐛 Bug 修复

            - 修复数据库连接泄漏问题（所有操作添加 try-finally）
            - 修复 API 输入验证缺失问题
            - 修复前端重复代码问题

            ## 使用说明

            1. 下载对应平台的安装包
            2. 双击安装
            3. 启动应用即可使用

            数据存储位置：
            - 数据库：`~/.fundval-live/data/fund.db`
            - 日志：`~/.fundval-live/logs/`

            ## 注意事项

            - **macOS**：
              - 首次打开可能提示"无法验证开发者"，请在系统偏好设置 → 安全性与隐私中允许
              - 如提示"应用已损坏"，在终端执行：
                ```bash
                sudo xattr -dr com.apple.quarantine "/Applications/FundVal Live.app"
                ```
            - **Windows**：可能提示 SmartScreen，点击"更多信息"→"仍要运行"
            - **Linux**：AppImage 需要添加执行权限：`chmod +x FundVal-Live-*.AppImage`

            ## 开源协议

            本项目采用 AGPL-3.0 协议开源。如果你使用本项目代码提供网络服务，必须开源你的修改。
            详见：https://github.com/Ye-Yu-Mo/FundVal-Live/blob/main/LICENSE
          files: |
            macos-build/*
            windows-build/*
            linux-build/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
